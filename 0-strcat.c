#include "main.h"

/**
 * *_strcat - concatenates two strings
 *
 * Return (ptr)
 */

char *_strcat(char *dest, char *src)

{
char *ptr; 
_strcat(dest, src);
ptr = _strcat(dest, src);
printf("%s", ptr);

return (0);
}
